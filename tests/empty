$config = {
    tests     => 1,
    defaultui => 'default',

    UI => {
        default => {
            template   => "wall",
            updatetime => 1000,    #ns
            checks => [ ]
          }

    }
};
